namespace AuthApiSesh.Enums
{
    public enum RegisterErrors
    {
        email,
        username,
        password

    }

    public enum TokenType
    {
        Access,
        Refresh,
        Confirm
    }

}
